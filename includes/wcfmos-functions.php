<?php
/**
 * This file is used for writing all the re-usable custom functions.
 *
 * @since   1.0.0
 * @package WCFMOS_Order_Splitter
 * @subpackage WCFMOS_Order_Splitter/includes
 */
defined( 'ABSPATH' ) || exit; // Exit if accessed directly.

/**
 * Check if, `wcfmos_split_order` function exists.
 */
if ( ! function_exists( 'wcfmos_split_order' ) ) {
	/**
	 * Split the order.
	 *
	 * @param int $order_id WooCommerce order ID.
	 */
	function wcfmos_split_order( $order_id ) {
	}
}
